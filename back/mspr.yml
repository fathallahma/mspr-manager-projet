version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8081  # ou 8080 si tu l’as utilisé
functions:
  generate-password:
    image: fathallahma/mspr:latest
    lang: java17
    environment:
      write_debug: true

